{
  "name": "sql-placeholder",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn parcel build src/index.html",
    "start": "yarn parcel src/index.html",
    "format": "yarn prettier --write src/**/*.ts && yarn prettier --write src/**/*.html && yarn elm-format --yes src"
  },
  "devDependencies": {
    "@parcel/transformer-elm": "2.12.0",
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7",
    "parcel": "^2.12.0",
    "prettier": "^3.3.3"
  }
}
